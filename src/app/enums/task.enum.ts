export enum Priority {
  LOW = 'Low',
  HIGH = 'High',
  CRITICAL = 'Critical'
}

export enum Status {
  NOT_STARTED = 'Not Started',
  IN_PROGRESS = 'In Progress',
  COMPLETE = 'Complete',
  CANCELLED = 'Cancelled',
  DONE = 'Done',
  NOT_DONE = 'Not Done'
}